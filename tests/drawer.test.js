import { assert } from 'chai';
import { Drawer } from '../dist/terrain-profile';
import { lineString, multiLineString } from './data';

describe('Drawer tests', () => {
  it('Should draw', () => {
    // let drawer = new Drawer(null, multiLineString);
    // drawer.getSVG();
  });
});
